export function ShopToolbar({ count, sort, setSort }: { count:number; sort:string; setSort:(v:string)=>void }) {
  return <div className="mb-4 flex items-center justify-between gap-3 rounded-xl border border-borderBlue bg-white p-3 shadow-soft sm:p-4"><p className="text-xs font-black sm:text-sm">{new Intl.NumberFormat("fa-IR").format(count)} محصول پیدا شد</p><label className="flex items-center gap-2 text-xs font-bold"><span className="hidden sm:inline">مرتب‌سازی:</span><select aria-label="مرتب‌سازی محصولات" value={sort} onChange={e=>setSort(e.target.value)} className="h-10 rounded-lg border border-borderBlue bg-white px-3 outline-none focus:border-royal"><option value="popular">محبوب‌ترین</option><option value="cheap">ارزان‌ترین</option><option value="expensive">گران‌ترین</option><option value="rating">بالاترین امتیاز</option></select></label></div>;
}
