"use client";
import { useState } from "react";
const faqs = [
  ["چطور از اصالت کالا مطمئن شوم؟", "تمام محصولات با تضمین اصالت کاغذ ۲۰ عرضه می‌شوند و مشخصات فنی هر کالا پیش از خرید در صفحه محصول درج شده است."],
  ["سفارش چه زمانی ارسال می‌شود؟", "سفارش‌های تأییدشده در روزهای کاری پس از هماهنگی، بسته‌بندی و تحویل واحد ارسال می‌شوند. زمان دقیق با توجه به مقصد اعلام می‌شود."],
  ["امکان خرید عمده وجود دارد؟", "بله؛ برای سفارش سازمانی و عمده می‌توانید از صفحه محصول یا بخش تماس با ما درخواست استعلام قیمت ثبت کنید."],
  ["چطور وضعیت سفارش را پیگیری کنم؟", "با شماره سفارش و موبایل ثبت‌شده می‌توانید از صفحه پیگیری سفارش استفاده کنید یا با پشتیبانی تماس بگیرید."],
  ["آیا امکان مرجوعی کالا وجود دارد؟", "در صورت مغایرت یا آسیب‌دیدگی، حداکثر تا ۲۴ ساعت پس از تحویل موضوع را با تصاویر بسته‌بندی به پشتیبانی اطلاع دهید."],
  ["چه روش‌هایی برای پرداخت وجود دارد؟", "پرداخت اینترنتی امن و هماهنگی پرداخت برای سفارش‌های عمده در دسترس است. جزئیات در صفحه رویه‌های پرداخت آمده است."],
];
export function FaqAccordion() { const [open, setOpen] = useState(0); return <section className="faq-accordion" aria-label="پرسش‌های متداول">{faqs.map(([q,a], index) => <article className={open === index ? "open" : ""} key={q}><button type="button" onClick={() => setOpen(open === index ? -1 : index)} aria-expanded={open === index}><span>{q}</span><b>{open === index ? "−" : "+"}</b></button>{open === index && <p>{a}</p>}</article>)}</section>; }
