# Backend URL used by the Next.js server (product pages, sitemap, account panel).
PAPER_API_URL=https://www.kaghaz20.ir

# Backend URL the browser calls directly (login, cart, payment, admin panel).
# There is no Next.js proxy; the backend sets the paper_token cookie itself.
NEXT_PUBLIC_API_URL=https://www.kaghaz20.ir
